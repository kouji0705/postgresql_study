services:
  db:
    image: postgres:16
    container_name: pg-lab
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: index_lab
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
